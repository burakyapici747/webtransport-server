class Player {
  constructor(socketId, username, color, x, y, rotation) {
    this.socketId = socketId;
    this.username = username;
    this.color = color;
    this.x = x;
    this.y = y;
    this.rotation = rotation;
  }
}